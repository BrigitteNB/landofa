<h1>Bonjour!</h1>

<p>Pour réinitialiser votre mot de passe, veuillez cliquer sur le lien ci-desous</p>

<a href="{{ url('visitor.authentication.reset_password', {token: resetToken.token}) }}">{{ url('visitor.authentication.reset_password', {token: resetToken.token}) }}</a>

<p>Ce lien expirera le {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>A bientôt!</p>
